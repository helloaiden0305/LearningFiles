问题一：如何处理高频推送的数据流？（涉及 WebSocket、Protobuf、OkHttp）
核心思路：全双工通信 + 高效序列化 + 数据流控
为什么选择 WebSocket 而不是轮询？
股票行情要求毫秒级延迟。传统的 HTTP 轮询会产生大量的 Header 开销，且无法做到真正的实时。
WebSocket 提供了基于 TCP 的全双工长连接，服务器可以主动向客户端推送数据，极大降低了网络开销和延迟。在 Android 中，通常直接使用 OkHttp 提供的 WebSocket 支持来实现。
为什么使用 Protobuf 而不是 JSON？
体积小、解析快：Protobuf 是基于二进制流的序列化协议。相比 JSON，它的体积通常只有 1/3 到 1/10，解析速度快几十倍。
节省带宽与电量：在每秒几十次的高频推送下，使用 Protobuf 能显著降低用户的流量消耗，同时减少 CPU 解析数据的耗时，从而降低手机发热和耗电。
OkHttp 拦截器的应用
面试官可能会问网络层的统一处理。你可以回答：通过自定义 OkHttp Interceptor（拦截器），可以在底层统一处理请求头（如鉴权 Token）、统一打印网络日志、或者进行全局的错误码拦截与重试逻辑。

问题二：如何保证长连接的稳定性，以及弱网环境下的重连策略？（涉及心跳保活、弱网重连）
核心思路：及时感知断连 + 智能退避重连 + 节省系统资源
心跳保活机制（Heartbeat）
为什么需要心跳？ 主要是为了应对运营商的 NAT 穿透超时（长时间无数据传输，运营商路由器会强制断开连接）以及半连接状态（如拔掉网线，系统未及时发出 FIN 包，导致假在线）。
实现方案：客户端定时（例如每 30 秒）向服务器发送一个极小的 Ping 包，服务器回复 Pong 包。如果连续 N 次未收到响应，则认为连接已断开，主动触发重连。
弱网环境下的重连策略（核心考点）
指数退避算法（Exponential Backoff）：断网后绝对不能无限高频重连，这会导致手机耗电剧增，且在服务器故障时引发“雪崩效应”。正确的做法是：重连间隔时间逐渐增加（如 2s, 4s, 8s, 16s...），达到最大阈值后停止或保持低频重连。
网络状态监听：注册系统网络状态变化广播（或使用 ConnectivityManager.NetworkCallback）。当监听到网络从无到有（如走出电梯恢复 4G/WiFi）时，立即打断退避休眠，立刻发起一次重连。
前后台生命周期管理
节省资源：当 App 退到后台时，用户不需要看实时行情。此时应该降低心跳频率，或者在后台超过一定时间后主动断开 WebSocket 连接。
当 App 重新回到前台（onResume）时，立即检查连接状态，若已断开则迅速重连并拉取最新快照数据，保证用户体验。

问题三：如何高效绘制复杂的证券图表（K线图、分时图）并处理复杂交互？（涉及 Canvas 绘制、事件分发、SurfaceView）
核心思路：独立线程渲染 + 局部绘制优化 + 剥夺父控件拦截权
为什么使用 SurfaceView 而不是普通 View？
普通 View 的 onDraw 运行在主线程。在 120Hz 高刷屏下（每帧仅约 
8.33
m
s
8.33ms），绘制海量 K 线数据极易阻塞主线程导致掉帧（Jank）。SurfaceView 拥有独立的双缓冲渲染机制，可以在子线程中通过 lockCanvas() 进行高频绘制，完成后提交，彻底解放主线程，保证 UI 的丝滑。
如何处理图表缩放与十字光标的滑动冲突？
K线图通常嵌套在 ScrollView 或 ViewPager 中。当用户单指长按（唤出十字光标）或双指缩放（结合 ScaleGestureDetector）时，必须在自定义 View 的 onTouchEvent 中调用 parent.requestDisallowInterceptTouchEvent(true)，强制剥夺父控件的事件拦截权，确保图表内部的滑动和缩放体验不受外层控件干扰。
海量图表元素的绘制优化策略
避免对象分配：绝对不要在 onDraw 中频繁创建对象（如 new Paint() 或 new Path()），防止频繁触发 GC 造成卡顿。
可见区域绘制：计算当前屏幕可见的 K 线数据索引（startIndex 到 endIndex），只绘制屏幕内的数据。使用 Path 批量绘制折线（如均线），而不是循环调用 drawLine。

问题四：如何优雅地处理后台海量推送数据并刷新 UI？（涉及 协程、Flow/RxJava、线程同步）
核心思路：子线程解析 + 数据流控（背压） + 线程安全
如何平衡高频数据推送与 UI 渲染（背压处理）？
后台可能每秒推送 50 次行情数据，但屏幕刷新率有限，全量刷新会导致 UI 卡死。在使用 Kotlin Flow（或 RxJava）时，必须进行数据流控。可以通过 conflate() 操作符（只保留最新数据，丢弃中间积压数据）或 sample() / debounce() 进行限流降频，将刷新频率控制在人眼舒适的范围（如 3Hz 到 5Hz）。
协程在网络与数据解析中的应用
WebSocket 接收到二进制流后，必须在 Dispatchers.IO 线程池中进行 Protobuf 的反序列化和数据组装。处理完毕后，通过 withContext(Dispatchers.Main) 或 StateFlow 将最终的 UI 状态安全地抛回主线程进行渲染。
多线程环境下的数据同步与锁机制
当多个子线程同时更新全市场几千只股票的缓存字典时，如果直接使用普通 HashMap 会触发 ConcurrentModificationException。必须使用 ConcurrentHashMap，或者在协程环境下使用 Mutex（互斥锁） 来保证共享状态的一致性和线程安全。

问题五：如何解决行情列表的滑动卡顿与长时间盯盘导致的 OOM？（涉及 RecyclerView 优化、内存泄漏排查）
核心思路：极致局部刷新 + 严控对象生命周期
RecyclerView 的极致优化（Payload 机制）
当某只股票价格跳动时，绝对禁止调用 notifyDataSetChanged() 或无参数的 notifyItemChanged(position)，这会导致整个 Item 重绘并闪烁。必须重写带有 payload 参数的 onBindViewHolder，结合 DiffUtil，将变化的价格字段作为 payload 传入，精准地只更新那一个 TextView。同时固定价格控件的宽高，避免文字跳动触发耗时的 requestLayout。
长时间挂机盯盘的内存泄漏（OOM）治理
金融 App 极易因为 WebSocket 回调、单例模式或匿名内部类非法持有 Activity 的 Context 而导致内存泄漏。解决方案是：在生命周期 onDestroy 中严格注销所有网络监听。日常开发接入 LeakCanary，线上发生 OOM 时，抓取 hprof 文件并使用 MAT 工具 分析 GC Root 引用链，精准定位泄漏源。

问题六：如何设计架构以应对繁杂的金融业务并保证状态一致性？（涉及 MVI/MVVM、组件化）
核心思路：单向数据流 + 物理隔离解耦
为什么在金融 App 中推荐 MVI 架构？
金融交易对状态的准确性要求极高（如未登录、请求中、持仓为空）。MVVM 中多个 LiveData 容易导致状态割裂和时序错乱。MVI 架构强调单向数据流（UDF）和唯一可信数据源。ViewModel 产出唯一的 UiState，UI 层只负责根据这个 State 渲染画面，用户的任何交互都封装成 Intent 发送给 ViewModel，彻底杜绝状态错乱。
组件化/模块化开发思想
将 App 拆分为“行情”、“交易”、“资讯”、“开户”等独立 Module。底层抽取网络、数据库等 Base 库。模块之间绝对禁止直接依赖，页面跳转使用 ARouter，服务调用通过接口下沉（在 Common 层定义接口，业务层实现），从而提升编译速度，方便多团队并行开发。

问题七：如何保障用户的资金安全与交易数据防篡改？（涉及 加密算法、防抓包、本地安全存储）
核心思路：混合加密 + 证书锁定 + 硬件级存储
网络传输的混合加密机制
交易报文绝不能裸奔。通常采用 “RSA + AES” 混合加密方案：利用非对称加密 RSA 安全地交换 AES 的对称密钥，然后用 AES 加密实际的业务数据。这样既保证了密钥分发的安全性，又兼顾了海量数据加解密的性能。
防抓包与防中间人攻击（Certificate Pinning）
为了防止黑客使用 Charles 等工具进行中间人伪造攻击，必须在 OkHttp 中配置 CertificatePinner（证书锁定）。将服务器证书的公钥 Hash 值硬编码在 App 中，建立连接时强制校验。一旦发现证书 Hash 不匹配，直接断开连接，彻底阻断抓包。
本地敏感数据的安全存储
用户的 Token、交易密码绝对不能明文保存在 SharedPreferences 中。必须使用 Android 官方的 EncryptedSharedPreferences，其底层依赖 Android Keystore 系统，密钥存储在硬件安全模块（TEE）中。核心加密逻辑应使用 NDK 编写 C/C++ 代码 并加固，防止被 JADX 轻易反编译。
